@using SpirithubCafe.Web.Services
@using SpirithubCafe.Domain.Entities
@using SpirithubCafe.Application.Services
@using System.Timers
@using Microsoft.JSInterop
@inject IChatService ChatService
@inject ISettingService SettingService
@inject TranslationLocalizer Localizer
@inject ToastService ToastService
@inject IJSRuntime JSRuntime
@implements IDisposable



@if (isChatWidgetEnabled)
{
<div class="fixed bottom-24 right-4 z-[60] modern-chat-widget md:bottom-4">
    @if (!isExpanded)
    {
        <button @onclick="ToggleChat" 
                class="modern-chat-button group relative rounded-full shadow-lg transition-all duration-300 hover:scale-105 flex items-center justify-center w-16 h-16 p-4">
            @if (hasUnreadMessages)
            {
                <div class="absolute -top-1 -right-1 bg-red-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center animate-bounce font-medium">
                    !
                </div>
            }
            <svg class="w-6 h-6 text-white group-hover:scale-110 transition-transform duration-200" viewBox="0 0 24 24" fill="none">
                <path d="M13.0867 21.3877L13.7321 21.7697L13.0867 21.3877ZM13.6288 20.4718L12.9833 20.0898L13.6288 20.4718ZM10.3712 20.4718L9.72579 20.8539H9.72579L10.3712 20.4718ZM10.9133 21.3877L11.5587 21.0057L10.9133 21.3877ZM2.3806 15.9134L3.07351 15.6264V15.6264L2.3806 15.9134ZM7.78958 18.9915L7.77666 19.7413L7.78958 18.9915ZM5.08658 18.6194L4.79957 19.3123H4.79957L5.08658 18.6194ZM21.6194 15.9134L22.3123 16.2004V16.2004L21.6194 15.9134ZM16.2104 18.9915L16.1975 18.2416L16.2104 18.9915ZM18.9134 18.6194L19.2004 19.3123H19.2004L18.9134 18.6194ZM19.6125 2.7368L19.2206 3.37628L19.6125 2.7368ZM21.2632 4.38751L21.9027 3.99563V3.99563L21.2632 4.38751ZM4.38751 2.7368L3.99563 2.09732V2.09732L4.38751 2.7368ZM2.7368 4.38751L2.09732 3.99563H2.09732L2.7368 4.38751ZM9.40279 19.2098L9.77986 18.5615L9.77986 18.5615L9.40279 19.2098ZM13.7321 21.7697L14.2742 20.8539L12.9833 20.0898L12.4412 21.0057L13.7321 21.7697ZM9.72579 20.8539L10.2679 21.7697L11.5587 21.0057L11.0166 20.0898L9.72579 20.8539ZM12.4412 21.0057C12.2485 21.3313 11.7515 21.3313 11.5587 21.0057L10.2679 21.7697C11.0415 23.0767 12.9585 23.0767 13.7321 21.7697L12.4412 21.0057ZM10.5 2.75H13.5V1.25H10.5V2.75ZM21.25 10.5V11.5H22.75V10.5H21.25ZM2.75 11.5V10.5H1.25V11.5H2.75ZM1.25 11.5C1.25 12.6546 1.24959 13.5581 1.29931 14.2868C1.3495 15.0223 1.45323 15.6344 1.68769 16.2004L3.07351 15.6264C2.92737 15.2736 2.84081 14.8438 2.79584 14.1847C2.75041 13.5189 2.75 12.6751 2.75 11.5H1.25ZM7.8025 18.2416C6.54706 18.2199 5.88923 18.1401 5.37359 17.9265L4.79957 19.3123C5.60454 19.6457 6.52138 19.7197 7.77666 19.7413L7.8025 18.2416ZM1.68769 16.2004C2.27128 17.6093 3.39066 18.7287 4.79957 19.3123L5.3736 17.9265C4.33223 17.4951 3.50486 16.6678 3.07351 15.6264L1.68769 16.2004ZM21.25 11.5C21.25 12.6751 21.2496 
16" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                <path d="M8 12.5H13.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
            </svg>
        </button>
    }
    else
    {
        <div class="modern-chat-container bg-white rounded-2xl shadow-2xl w-80 sm:w-96 flex flex-col border border-gray-100 overflow-hidden" style="height: auto; max-height: 90vh;">
            <!-- Header -->
            <div class="modern-chat-header text-white p-3 sm:p-4 flex justify-between items-center relative">
                <div class="flex items-center space-x-2 sm:space-x-3 relative z-10">
                    <div class="w-2.5 h-2.5 sm:w-3 sm:h-3 bg-green-400 rounded-full animate-pulse shadow-lg shadow-green-400/50"></div>
                    <h3 class="font-bold text-base sm:text-lg">@Localizer["ChatWithUs"]</h3>
                </div>
                <div class="flex items-center space-x-1 sm:space-x-2 relative z-10">
                    @if (currentSession != null)
                    {
                        <button @onclick="EndChatSession" class="text-white hover:text-red-200 text-xs px-3 py-1.5 bg-white/20 backdrop-blur-sm rounded-full transition-all duration-200 font-medium hover:bg-red-500/20">
                            End Chat
                        </button>
                    }
                    <button @onclick="ToggleChat" class="text-white hover:text-gray-200 p-1.5 rounded-full hover:bg-white/10 transition-all duration-200">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                        </svg>
                    </button>
                </div>
            </div>

            <!-- Content -->
            <div class="flex-1 flex flex-col">
                @if (currentSession == null)
                {
                    <!-- Start Chat Form -->
                    <div class="modern-chat-form-container p-4 sm:p-6 flex flex-col overflow-y-auto" style="height: 420px; min-height: 420px;">
                        <style>
                            .modern-chat-form-container {
                                scrollbar-width: thin;
                                scrollbar-color: #37353E #f9fafb;
                            }
                            .modern-chat-form-container::-webkit-scrollbar {
                                width: 6px;
                            }
                            .modern-chat-form-container::-webkit-scrollbar-track {
                                background: #f9fafb;
                                border-radius: 6px;
                            }
                            .modern-chat-form-container::-webkit-scrollbar-thumb {
                                background: #37353E;
                                border-radius: 6px;
                            }
                            .modern-chat-form-container::-webkit-scrollbar-thumb:hover {
                                background: #715A5A;
                            }
                            .modern-chat-messages-container {
                                scrollbar-width: thin;
                                scrollbar-color: #37353E #f9fafb;
                            }
                            .modern-chat-messages-container::-webkit-scrollbar {
                                width: 6px;
                            }
                            .modern-chat-messages-container::-webkit-scrollbar-track {
                                background: #f9fafb;
                                border-radius: 6px;
                            }
                            .modern-chat-messages-container::-webkit-scrollbar-thumb {
                                background: #37353E;
                                border-radius: 6px;
                            }
                            .modern-chat-messages-container::-webkit-scrollbar-thumb:hover {
                                background: #715A5A;
                            }
                        </style>
                        <div class="text-center mb-6">
                            <div class="modern-chat-avatar w-16 h-16 rounded-2xl flex items-center justify-center mx-auto mb-3 shadow-lg">
                                <svg class="w-10 h-10 text-white" viewBox="0 0 24 24" fill="none">
                                    <path d="M13.0867 21.3877L13.7321 21.7697L13.0867 21.3877ZM13.6288 20.4718L12.9833 20.0898L13.6288 20.4718ZM10.3712 20.4718L9.72579 20.8539H9.72579L10.3712 20.4718ZM10.9133 21.3877L11.5587 21.0057L10.9133 21.3877ZM2.3806 15.9134L3.07351 15.6264V15.6264L2.3806 15.9134ZM7.78958 18.9915L7.77666 19.7413L7.78958 18.9915ZM5.08658 18.6194L4.79957 19.3123H4.79957L5.08658 18.6194ZM21.6194 15.9134L22.3123 16.2004V16.2004L21.6194 15.9134ZM16.2104 18.9915L16.1975 18.2416L16.2104 18.9915ZM18.9134 18.6194L19.2004 19.3123H19.2004L18.9134 18.6194ZM19.6125 2.7368L19
.73809 2.83873C7.663 2.75079 8.84876 2.75 10.5 2.75V1.25ZM2.75 10.5C2.75 8.84876 2.75079 7.663 2.83873 6.73809C2.92561 5.82434 3.09223 5.24291 3.37628 4.77938L2.09732 3.99563C1.64457 4.73445 1.44279 5.57256 1.34547 6.59611C1.24921 7.6085 1.25 8.87781 1.25 10.5H2.75ZM3.99563 2.09732C3.22194 2.57144 2.57144 3.22194 2.09732 3.99563L3.37628 4.77938C3.72672 4.20752 4.20752 3.72672 4.77938 3.37628L3.99563 2.09732ZM11.0166 20.0898C10.8136 19.7468 10.6354 19.4441 10.4621 19.2063C10.2795 18.9559 10.0702 18.7304 9.77986 18.5615L9.02572 19.8582C9.07313 19.8857 9.13772 19.936 9.24985 20.0898C9.37122 20.2564 9.50835 20.4865 9.72579 20.8539L11.0166 20.0898ZM7.77666 19.7413C8.21575 19.7489 8.49387 19.7545 8.70588 19.7779C8.90399 19.7999 8.98078 19.832 9.02572 19.8582L9.77986 18.5615C9.4871 18.3912 9.18246 18.3215 8.87097 18.287C8.57339 18.2541 8.21375 18.2487 7.8025 18.2416L7.77666 19.7413ZM14.2742 20.8539C14.4916 20.4865 14.6287 20.2564 14.7501 20.0898C14.8622 19.936 14.9268 19.8857 14.9742 19.8582L14.2201 18.5615C13.9298 18.7304 13.7204 18.9559 13.5379 19.2063C13.3646 19.4441 13.1864 19.7468 12.9833 20.0898L14.2742 20.8539ZM16.1975 18.2416C15.7862 18.2487 15.4266 18.2541 15.129 18.287C14.8175 18.3215 14.5129 18.3912 14.2201 18.5615L14.9742 19.8582C15.0192 19.832 15.096 19.7999 15.2941 19.7779C15.5061 19.7545 15.7842 19.7489 16.2233 19.7413L16.1975 18.2416Z" fill="currentColor"/>
                                    <path d="M8 9H16" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                                    <path d="M8 12.5H13.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                                </svg>
                            </div>
                            <h4 class="text-lg font-bold text-gray-800 mb-2">@Localizer["StartChat"]</h4>
                            <p class="text-xs text-gray-600 leading-relaxed">We're here to help! Start a conversation with us and get instant support.</p>
                        </div>
                        
                        <div class="space-y-3 flex-1 flex flex-col">
                            <div>
                                <label class="block text-sm font-semibold text-gray-800 mb-2">@Localizer["YourName"] *</label>
                                <input @bind="chatForm.VisitorName" 
                                       class="modern-chat-input w-full px-3 py-2.5 border border-gray-200 rounded-xl transition-all duration-200 focus:outline-none focus:ring-2 focus:border-transparent text-sm" 
                                       required />
                            </div>
                        
                            <div class="grid grid-cols-1 gap-3">
                                <div>
                                    <label class="block text-sm font-semibold text-gray-800 mb-2">@Localizer["YourEmail"]</label>
                                    <input @bind="chatForm.VisitorEmail" 
                                           placeholder="example@email.com"
                                           type="email"
                                           class="modern-chat-input w-full px-3 py-2.5 border border-gray-200 rounded-xl transition-all duration-200 focus:outline-none focus:ring-2 focus:border-transparent text-sm" />
                                </div>
                                
                                <div>
                                    <label class="block text-sm font-semibold text-gray-800 mb-2">@Localizer["PhoneNumber"]</label>
                                    <input @bind="chatForm.PhoneNumber" 
                                           placeholder="+1 234 567 8900"
                                           type="tel"
                                           class="modern-chat-input w-full px-3 py-2.5 border border-gray-200 rounded-xl transition-all duration-200 focus:outline-none focus:ring-2 focus:border-transparent text-sm" />
                                </div>
                            </div>
                            
                            <div class="text-xs text-gray-600 text-center modern-info-box p-3 rounded-xl">
                                <div class="flex items-center justify-center gap-2">
                                    <svg class="w-4 h-4 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                    </svg>
                                    @Localizer["EmailOrPhoneRequired"]
                                </div>
                            </div>
                        
                            <button @onclick="StartChatSession" 
                                    disabled="@(isLoading || !chatForm.IsContactInfoValid || string.IsNullOrWhiteSpace(chatForm.VisitorName))"
                                    class="modern-chat-start-button w-full text-white py-3 rounded-xl transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed font-semibold text-sm hover:shadow-lg transform hover:scale-[1.02] active:scale-[0.98] mt-auto">
                                @if (isLoading)
                                {
                                    <div class="flex items-center justify-center">
                                        <svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                                            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                                            <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                                        </svg>
                                        <span>@Localizer["Loading"]...</span>
                                    </div>
                                }
                                else
                                {
                                    <span>💬 @Localizer["StartChat"]</span>
                                }
                            </button>
                        </div>
                    </div>
                }
                else
                {
                    <!-- Chat Messages -->
                    <div class="modern-chat-messages-container p-2 sm:p-3 overflow-y-auto bg-gray-50/20" @ref="messagesContainer" style="height: 360px; min-height: 360px;">
                        @if (messages.Any())
                        {
                            @foreach (var message in messages)
                            {
                                <div class="mb-3 @(message.SenderType == ChatSenderType.Visitor ? "text-right" : "text-left") animate-fade-in">
                                    <div class="inline-block max-w-[260px] sm:max-w-[300px] md:max-w-[320px] px-3 py-2 sm:px-4 sm:py-3 rounded-2xl shadow-sm @(message.SenderType == ChatSenderType.Visitor ? "modern-visitor-message text-white" : "bg-white text-gray-800 border border-gray-100 shadow-md")">
                                        <div class="text-sm leading-relaxed">@message.Content</div>
                                        <div class="text-xs opacity-70 mt-1.5 @(message.SenderType == ChatSenderType.Visitor ? "" : "text-gray-500")" style="@(message.SenderType == ChatSenderType.Visitor ? "color: #D3DAD9;" : "")">
                                            @message.SenderName • @message.CreatedAt.ToString("HH:mm")
                                        </div>
                                    </div>
                                </div>
                            }
                            @if (isTyping)
                            {
                                <div class="mb-4 text-left animate-fade-in">
                                    <div class="inline-block bg-white border border-gray-100 px-6 py-4 rounded-2xl shadow-md">
                                        <div class="flex space-x-1.5 items-center">
                                            <span class="text-xs text-gray-500 mr-2">Typing</span>
                                            <div class="w-2 h-2 rounded-full animate-bounce" style="background-color: #37353E;"></div>
                                            <div class="w-2 h-2 rounded-full animate-bounce" style="background-color: #44444E; animation-delay: 0.1s;"></div>
                                            <div class="w-2 h-2 rounded-full animate-bounce" style="background-color: #715A5A; animation-delay: 0.2s;"></div>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="flex items-center justify-center h-full">
                                <div class="text-center text-gray-500 text-sm px-4">
                                    <div class="w-12 h-12 modern-empty-chat rounded-2xl flex items-center justify-center mx-auto mb-3 shadow-sm">
                                        <svg class="w-6 h-6 text-white" viewBox="0 0 24 24" fill="none">
                                            <path d="M8 9H16" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                                            <path d="M8 12.5H13.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
                                        </svg>
                                    </div>
                                    <p class="text-gray-600 font-medium text-xs">@Localizer["TypeYourMessage"]...</p>
                                    <p class="text-xs text-gray-400 mt-1">Start the conversation!</p>
                                </div>
                            </div>
                        }
                    </div>

                    <!-- Message Input -->
                    <div class="p-2 sm:p-3 border-t border-gray-100 bg-white">
                        <div class="flex gap-2 items-center">
                            <div class="flex-1 relative">
                                <textarea @bind="messageForm.Content" 
                                         @onkeypress="@HandleKeyPress"
                                         placeholder="@Localizer["TypeYourMessage"]..."
                                         rows="1"
                                         class="modern-message-input w-full px-3 py-2 border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:border-transparent resize-none transition-all duration-200 text-sm"
                                         disabled="@isLoading"
                                         style="max-height: 60px; min-height: 36px;"></textarea>
                            </div>
                            <button @onclick="SendMessage" 
                                    disabled="@(isLoading || string.IsNullOrWhiteSpace(messageForm.Content))"
                                    class="modern-send-button w-9 h-9 text-white rounded-xl transition-all duration-200 disabled:opacity-50 flex items-center justify-center shadow-md hover:shadow-lg transform hover:scale-105 active:scale-95">
                                @if (isLoading)
                                {
                                    <svg class="animate-spin w-4 h-4" fill="none" viewBox="0 0 24 24">
                                        <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                                        <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                                    </svg>
                                }
                                else
                                {
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
                                    </svg>
                                }
                            </button>
                        </div>
                    </div>
                }
            </div>
        </div>
    }
</div>
}

@code {
    private bool isExpanded = false;
    private bool isLoading = false;
    private bool isChatWidgetEnabled = true;
    private bool hasUnreadMessages = false;
    private bool isTyping = false;
    private ChatSession? currentSession;
    private List<ChatMessage> messages = new();
    private ElementReference messagesContainer;
    private ChatForm chatForm = new();
    private MessageForm messageForm = new();
    private Timer? refreshTimer;
    private Timer? typingTimer;
    private DateTime lastMessageCheck = DateTime.Now;
    private string? storedSessionId;

    public class ChatForm
    {
        [Required]
        public string VisitorName { get; set; } = string.Empty;
        public string? VisitorEmail { get; set; }
        public string? PhoneNumber { get; set; }

        // Custom validation: Either email or phone must be provided
        public bool IsContactInfoValid => !string.IsNullOrWhiteSpace(VisitorEmail) || !string.IsNullOrWhiteSpace(PhoneNumber);
    }

    public class MessageForm
    {
        [Required]
        public string Content { get; set; } = string.Empty;
    }

    protected override async Task OnInitializedAsync()
    {
        // Check if chat widget is enabled
        var chatWidgetSetting = await SettingService.GetValueAsync("ChatWidgetEnabled", "true");
        isChatWidgetEnabled = chatWidgetSetting.Equals("true", StringComparison.OrdinalIgnoreCase);
        
        if (isChatWidgetEnabled)
        {
            // Try to restore existing session
            await RestoreSession();
            
            // Setup refresh timer for checking new messages
            refreshTimer = new Timer(3000); // Check every 3 seconds
            refreshTimer.Elapsed += async (sender, e) => await CheckForNewMessages();
            refreshTimer.AutoReset = true;
            refreshTimer.Enabled = true;
        }
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender && isChatWidgetEnabled)
        {
            // Restore expanded state
            try
            {
                var expanded = await JSRuntime.InvokeAsync<string>("localStorage.getItem", "chatWidget_expanded");
                if (!string.IsNullOrEmpty(expanded) && bool.TryParse(expanded, out var isExpandedStored))
                {
                    isExpanded = isExpandedStored;
                    StateHasChanged();
                }
            }
            catch (Exception ex)
            {
                Console.WriteLine($"Error restoring expanded state: {ex.Message}");
            }
        }
    }

    private async Task RestoreSession()
    {
        try
        {
            // Skip during prerendering
            if (JSRuntime is not IJSInProcessRuntime)
            {
                return;
            }
            
            storedSessionId = await JSRuntime.InvokeAsync<string>("localStorage.getItem", "chatWidget_sessionId");
            if (!string.IsNullOrEmpty(storedSessionId) && Guid.TryParse(storedSessionId, out var parsedSessionId))
            {
                // Try to get the session from the service
                var sessions = await ChatService.GetActiveSessionsAsync();
                currentSession = sessions.FirstOrDefault(s => s.Id == storedSessionId);
                
                if (currentSession != null)
                {
                    // Restore chat form data
                    chatForm.VisitorName = currentSession.VisitorName;
                    chatForm.VisitorEmail = currentSession.VisitorEmail;
                    chatForm.PhoneNumber = currentSession.PhoneNumber;
                    
                    await LoadMessages();
                }
                else
                {
                    // Session doesn't exist anymore, clear localStorage
                    await JSRuntime.InvokeVoidAsync("localStorage.removeItem", "chatWidget_sessionId");
                }
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error restoring session: {ex.Message}");
        }
    }

    private async Task CheckForNewMessages()
    {
        if (currentSession == null) return;
        
        try
        {
            var allMessages = await ChatService.GetMessagesAsync(currentSession.Id);
            var messagesList = allMessages.ToList();
            
            // Check for new admin messages
            var newAdminMessages = messagesList.Where(m => 
                m.SenderType == ChatSenderType.Admin && 
                m.CreatedAt > lastMessageCheck &&
                !messages.Any(existing => existing.Id == m.Id)
            ).ToList();
            
            if (newAdminMessages.Any())
            {
                foreach (var msg in newAdminMessages)
                {
                    messages.Add(msg);
                }
                
                // Show notification if chat is not expanded
                if (!isExpanded)
                {
                    hasUnreadMessages = true;
                }
                
                lastMessageCheck = DateTime.Now;
                await InvokeAsync(StateHasChanged);
                
                // Auto scroll to bottom
                await ScrollToBottom();
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error checking for new messages: {ex.Message}");
        }
    }

    private async Task ToggleChat()
    {
        isExpanded = !isExpanded;
        
        if (isExpanded)
        {
            hasUnreadMessages = false;
            await ScrollToBottom();
        }
        
        // Save expanded state to localStorage
        try
        {
            await JSRuntime.InvokeVoidAsync("localStorage.setItem", "chatWidget_expanded", isExpanded.ToString());
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error saving expanded state: {ex.Message}");
        }
        
        StateHasChanged();
    }

    private async Task EndChatSession()
    {
        try
        {
            // Clear localStorage
            await JSRuntime.InvokeVoidAsync("localStorage.removeItem", "chatWidget_sessionId");
            await JSRuntime.InvokeVoidAsync("localStorage.removeItem", "chatWidget_expanded");
            
            // Reset state
            currentSession = null;
            messages.Clear();
            chatForm = new ChatForm();
            messageForm = new MessageForm();
            hasUnreadMessages = false;
            
            ToastService.ShowSuccess("Chat session ended");
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error ending chat session: {ex.Message}");
        }
    }

    private async Task ScrollToBottom()
    {
        try
        {
            await Task.Delay(100); // Small delay to ensure DOM is updated
            await JSRuntime.InvokeVoidAsync("scrollToBottom", messagesContainer);
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error scrolling to bottom: {ex.Message}");
        }
    }

    private async Task StartChatSession()
    {
        if (isLoading) return;
        
        isLoading = true;
        StateHasChanged();

        try
        {
            if (string.IsNullOrWhiteSpace(chatForm.VisitorName))
            {
                ToastService.ShowError(Localizer["PleaseEnterYourName"]);
                return;
            }

            if (!chatForm.IsContactInfoValid)
            {
                ToastService.ShowError(Localizer["EmailOrPhoneRequired"]);
                return;
            }

            // Use a simple IP detection or default value
            var ipAddress = "127.0.0.1"; // Default for now
            
            currentSession = await ChatService.CreateChatSessionAsync(
                chatForm.VisitorName, 
                chatForm.VisitorEmail,
                chatForm.PhoneNumber,
                ipAddress);

            if (currentSession != null)
            {
                // Save session ID to localStorage
                await JSRuntime.InvokeVoidAsync("localStorage.setItem", "chatWidget_sessionId", currentSession.Id.ToString());
                
                await LoadMessages();
                ToastService.ShowSuccess(Localizer["ChatStartedSuccessfully"]);
            }
        }
        catch (Exception ex)
        {
            ToastService.ShowError($"Error: {ex.Message}");
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private async Task HandleKeyPress(KeyboardEventArgs e)
    {
        if (e.Key == "Enter" && !e.ShiftKey)
        {
            await SendMessage();
        }
    }

    private async Task SendMessage()
    {
        if (currentSession == null || isLoading || string.IsNullOrWhiteSpace(messageForm.Content))
            return;

        isLoading = true;
        var content = messageForm.Content.Trim();
        messageForm.Content = string.Empty;
        StateHasChanged();

        try
        {
            var message = await ChatService.SendMessageAsync(
                currentSession.Id,
                ChatSenderType.Visitor,
                currentSession.VisitorName,
                content);

            if (message != null)
            {
                messages.Add(message);
                lastMessageCheck = DateTime.Now;
                await ScrollToBottom();
                
                // Simulate typing indicator for admin response
                await SimulateTyping();
            }
        }
        catch (Exception ex)
        {
            ToastService.ShowError(Localizer["ErrorSendingMessage"]);
            Console.WriteLine($"Error sending message: {ex.Message}");
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }

    private Task SimulateTyping()
    {
        isTyping = true;
        StateHasChanged();
        
        typingTimer?.Dispose();
        typingTimer = new Timer(3000); // Hide typing indicator after 3 seconds
        typingTimer.Elapsed += (sender, e) =>
        {
            isTyping = false;
            InvokeAsync(StateHasChanged);
            typingTimer?.Dispose();
        };
        typingTimer.AutoReset = false;
        typingTimer.Enabled = true;
        
        return Task.CompletedTask;
    }

    private async Task LoadMessages()
    {
        if (currentSession == null) return;
        
        try
        {
            messages = (await ChatService.GetMessagesAsync(currentSession.Id)).ToList();
            lastMessageCheck = DateTime.Now;
            await ScrollToBottom();
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading messages: {ex.Message}");
        }
    }

    public void Dispose()
    {
        refreshTimer?.Dispose();
        typingTimer?.Dispose();
    }
}